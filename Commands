PO is the executable file

Commands:
- make

- ./PO input from keyboard 
    ex: ./PO

- ./PO filename opens the filename.sp2020 ( doesn't require to put the extension) 
    ex:./PO file

- ./PO < filename.sp2020 read automaticaly from filename.sp2020 ( need to specify the .sp2020 or other extension) 
    ex: ./PO < file.sp2020

Steps:
    1. Compilation:
        Run makefile: make
    2. Run the program: ./PO or ./PO file or ./PO < file.sp2020
    3. See the file.inorder, file.preorder, file.postorder